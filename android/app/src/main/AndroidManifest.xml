<?xml version="1.0" encoding="utf-8"?>
<!--
  ============================================================
   AndroidManifest.xml — Identity Hub NFC
   Ubicación: android/app/src/main/AndroidManifest.xml
  ============================================================
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ PERMISO 1: Acceso al hardware NFC -->
    <uses-permission android:name="android.permission.NFC" />

    <!--
      ✅ FEATURE NFC:
      - android:required="true"  → La app NO se instala en dispositivos sin NFC.
      - android:required="false" → La app funciona en todos; tú manejas el caso
                                   sin NFC en código (recomendado para demo).
    -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <!-- ✅ PERMISO: Acceso a Internet para abrir URLs con Linking -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <!-- Intent principal para que la app aparezca en el launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              ✅ INTENT FILTER NFC — Opcional pero recomendado:
              Lanza la app automáticamente al acercar cualquier tag NFC
              cuando la app está cerrada. Puedes dejarlo comentado si
              prefieres controlar el escaneo solo desde el botón.

            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            -->

        </activity>
    </application>

</manifest>
